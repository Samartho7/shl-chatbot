# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0

# ML / Retrieval  (no sentence-transformers — uses Gemini embedding API instead)
faiss-cpu>=1.7.4
numpy>=1.24.0

# LLM + Embeddings (single API for both generation and embedding)
google-genai>=2.0.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
